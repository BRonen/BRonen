---
interface Props {
  title: string;
  body: string;
  href: string;
}

const { href, title, body } = Astro.props;
---

<li
  class="p-1 rounded-xl w-full group
  text-latte-text hover:text-transparent
  bg-[length:400%] animate-oscillation hover:bg-gradient-to-r
  from-latte-mauve via-latte-magenta to-latte-mauve"
  style="box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);"
>
  <a
    href={href}
    class="block w-full h-full px-4 py-3
    rounded-lg group-hover:bg-white group-hover:opacity-80"
  >
    <h2
      class="text-xl font-bold bg-[length:400%] bg-clip-text bg-gradient-to-r
      from-latte-mauve via-latte-magenta to-latte-mauve animate-oscillation"
    >
      {title}
      <span>&rarr;</span>
    </h2>
    <p class="text-latte-text mt-2">
      {body}
    </p>
  </a>
</li>
